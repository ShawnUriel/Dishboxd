# AI usage log

| Date | Tool | What I used it for | What I checked or changed myself |
| --- | --- | --- | --- |
| 2026-09-23 | AI chat tool | Explaining a confusing browser console error when my React frontend fetched from Express. It turned out to be CORS, so I installed and configured the `cors` package. | Confirmed the fetch from `localhost:5173` to `/api/test` works. |
| 2026-09-23 | Claude Code | Scaffolding the Week 1 skeleton from my README (Vite + React Router pages, Express `server.js` with `/api/test`, `database_setup.sql`, `.gitignore`, `.env.example`), running the security checks and capturing the screenshots (kept in my workspace repo under `project/screenshots/`). | Reviewed the files and the filled-in `project/SECURITY-CHECKLIST.md` before committing. |
